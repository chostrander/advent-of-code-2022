/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package hoss.code.app;

import hoss.code.list.LinkedList;

import static hoss.code.utilities.StringUtils.join;
import static hoss.code.utilities.StringUtils.split;
import static hoss.code.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
